# Example Environment Configuration File
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control!

# Database Configuration
DB_HOST=localhost
DB_USERNAME=your_db_username
DB_PASSWORD=your_secure_db_password
DB_NAME=your_database_name

# Authentication Configuration
# Use a strong PIN or consider upgrading to a proper password system
AUTH_PIN=your_secure_pin

# Application Configuration
APP_ENV=production

# Rate Limiting Configuration
# Max failed login attempts before lockout
RATE_LIMIT_MAX_ATTEMPTS=5
# Time window in minutes for counting attempts
RATE_LIMIT_WINDOW_MINUTES=15
# How long to lock out after max attempts reached (minutes)
RATE_LIMIT_LOCKOUT_MINUTES=30

# Session Configuration
# How long user sessions last without activity (minutes)
SESSION_TIMEOUT_MINUTES=60

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual configuration values
# 3. Never commit .env to version control
# 4. Keep your credentials secure and use strong passwords
# 5. Adjust rate limiting settings based on your security needs